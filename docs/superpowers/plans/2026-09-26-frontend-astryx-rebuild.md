# Frontend Astryx Rebuild Implementation Plan

> **For agentic workers:** Execute inline with focused checkpoints. Follow the phase order below; each phase is independently reviewable and committed before starting the next.

**Goal:** Rebuild Tnega's React frontend progressively around Astryx while preserving its current workbench information architecture and user-visible behavior.

**Architecture:** Keep `App.tsx` as the application state and routing/selection composition layer. Migrate the shell first, then each existing surface by composing Astryx components at the UI boundary without moving data or session behavior into library wrappers. Keep application-specific components only for Tnega workflows or behavior that Astryx does not supply.

**Tech Stack:** React 19, TypeScript, Vite, Tailwind CSS 4, Astryx (`@astryxdesign/core`, theme package, StyleX peer dependency, CLI).

**Spec:** `docs/frontend-astryx-rebuild.md`

## Global Constraints

- Preserve left workspace/session navigation, the main conversation/project area, and the optional right tool panel.
- Prefer existing Astryx components; do not create a new component where Astryx provides a suitable one.
- Rebuild in the order overall layout, local components, full application integration.
- Remove obsolete presentation-only frontend tests when their corresponding UI is replaced; preserve reusable state, event, API, and persistence tests.
- Do not add tests that only restate reversible, low-impact implementation details.
- Commit each independently reviewable phase with a Conventional Commit message of at most 72 characters.

---

### Phase 1: Establish Astryx and rebuild the workbench shell

**Files:**
- Modify: `apps/web/package.json`, workspace lockfile
- Modify: `apps/web/src/main.tsx`, `apps/web/src/App.tsx`, `apps/web/src/styles.css`
- Modify: `apps/web/src/workbench/WorkbenchShell.tsx`
- Inspect and update only if generated by Astryx initialization: root `AGENTS.md` or `apps/web/AGENTS.md`
- Remove only: `apps/web/src/workbench/workbench.test.ts` if it no longer tests behavior retained by the new shell; preserve its workspace sidebar behavior cases for later review if still applicable

- [x] Add Astryx core, a theme, StyleX peer dependency, and CLI using the package manager already governing the workspace. Add the documented local CLI script and initialize Astryx agent guidance without overwriting existing instructions.
- [x] Query the installed CLI for the exact App Shell, Side Nav, layout, button, icon button, and tooltip APIs before changing the shell.
- [x] Replace the shell's bespoke top-level frame with the available Astryx shell/navigation primitives. Keep Tnega's existing sidebar content and `children` slot intact.
- [x] Preserve the collapsed-sidebar persistence, responsive navigation drawer, optional tool rail/panel, Escape close behavior, and accessible labels where the corresponding Astryx primitives permit.
- [x] Load the Astryx reset and theme with explicit cascade layer ordering alongside Tailwind. Preserve the app's light/dark/system preference behavior and existing theme persistence.
- [x] Run `pnpm --filter @tnega/web build` as the focused phase check; visually inspect desktop, mobile navigation, and the tool panel in the running app.
- [x] Commit only the Astryx setup and shell files as `feat(web): rebuild workbench shell with Astryx`.

### Phase 2: Migrate workspace and session navigation

**Files:**
- Modify: `apps/web/src/workbench/WorkspaceSidebar.tsx`, `apps/web/src/workbench/WorkspaceTree.tsx`
- Modify: relevant workspace navigation styles in `apps/web/src/styles.css`
- Remove or retain: relevant cases from `apps/web/src/workbench/workbench.test.ts` based on whether they exercise retained behavior rather than old markup

- [x] Use Astryx navigation, tree, menu, button, text input, and dialog components where their documented APIs cover current workspace, project, and session interactions. Keep the Tnega project rows because `ListItem`'s documented interaction model does not fit a primary navigation action plus a separate overflow menu; record this exception in the rebuild spec.
- [x] Keep Tnega-specific grouping, selection, project/session callbacks, and local-storage behavior in the existing application boundary.
- [x] Delete obsolete presentation-only tests for replaced shell markup; retain reusable workspace/session behavior cases and their updated tree interactions.
- [x] Run the web typecheck, focused retained navigation tests, and a web build at this substantial local-component milestone.
- [x] Commit navigation work and obsolete-test removal in independently reviewable commits.

### Phase 3: Migrate the conversation and project surfaces

**Files:**
- Modify: `apps/web/src/conversation/ChatView.tsx`, `Transcript.tsx`, `ToolActivity.tsx`, `Disclosure.tsx`, and `SubagentSidebar.tsx` as needed
- Modify: `apps/web/src/project/ProjectExperience.tsx`, `ThreadPanel.tsx`, and `SidePanels.tsx` as needed
- Modify: `apps/web/src/workbench/ComposerFrame.tsx`, `SettingsView.tsx`, `apps/web/src/PlanPanel.tsx`, and their corresponding styles as needed
- Remove only obsolete presentation tests among `App.test.ts`, `conversation/*.test.ts`, and `projectExperience.test.ts`

- [x] Query Astryx docs for Chat Layout, Chat Message, Chat Composer, Chat Tool Calls, Markdown/Code, Tabs, forms, dialogs, and status components before migrating each matching surface.
- [x] Replace matching local visual controls with Astryx components while preserving streaming, durable session event projection, run controls, tool detail disclosure, project/thread workflows, settings saves, and plan/goal semantics.
- [x] Leave a surface in its current form and record the missing Astryx capability if no suitable library component exists; do not create a custom substitute solely for visual uniformity.
- [x] Remove tests tied only to replaced presentation; retain pure display projection, state, API, and interaction behavior tests with reusable assertions.
- [x] Run focused typecheck and relevant retained frontend behavior tests after each surface group.
- [x] Commit conversation/project migration in separate commits if each is independently reviewable.

## Recorded exceptions

Surfaces deliberately left on Tnega components, and why no Astryx component fits:

- **Slash-command menu** (`ChatView.tsx`). `ChatComposerInput`'s `triggers` is a flat, locally-searched typeahead; Tnega's menu is server-loaded and two-level (a command opens a submenu of argument candidates). It rides the composer's `drawer` slot instead.
- **Subagent card** (`Transcript.tsx`). Pairs a disclosure with a separate "open in sidebar" action; `Collapsible`'s trigger owns a single activation.
- **Conversation turn rail** (`ConversationNav.tsx`). A roving-focus marker rail with its own arrow/Home/End handling and a scroll-following effect.
- **Project rows** (`WorkspaceTree.tsx`, from Phase 2). A primary navigation action plus a separate overflow menu, which `ListItem`'s interaction model does not fit.


### Phase 4: Global integration and cleanup

**Files:**
- Modify: remaining `apps/web/src` UI and styles, `apps/web/README.md`
- Remove: only frontend test files proven obsolete after all surface migrations

- [ ] Search the web app for remaining Radix component imports, bespoke controls that duplicate Astryx, obsolete tokens, and unused CSS; remove only items superseded by the migration.
- [ ] Update the web README to describe the actual Astryx setup and current component boundaries.
- [ ] Run `pnpm --filter @tnega/web typecheck`, `pnpm --filter @tnega/web build`, then `pnpm test` for full integration. Fix any regressions before committing.
- [ ] Commit the remaining integration and cleanup as `refactor(web): complete Astryx frontend migration`.

## Self-review

- The preserved shell structure, component-first rule, test-cleanup rule, and staged order each map to the user-provided spec.
- The available Astryx component names are verified in its official component catalog; exact imports and props are intentionally checked against the installed CLI before implementation.
- Full-repository verification is reserved for the final phase; phase checks stay scoped to the web app.
